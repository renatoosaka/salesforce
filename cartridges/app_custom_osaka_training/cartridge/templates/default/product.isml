<strong>
  <isif condition="${product.productName}"> 
    ${product.productName}
  <iselse/>
    ${product.name}
  </isif>        
</strong>
<isif condition="${product.online}">
    <span>Online</span>
  <iselse/>
    <span>Offline</span>
</isif>
