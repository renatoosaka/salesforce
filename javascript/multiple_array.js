const mainArray = []

mainArray.push([1, 2, 3, 4])
mainArray.push(['a', 'b', 'c'])
mainArray.push([0, 9, 8])
mainArray.push(['i', 'o', 'a', 's', 'y', 's'])

for(const value of mainArray) {
  console.log(value)
}